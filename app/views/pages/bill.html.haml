.col-sm-12
  - if resource.blank?
    - content_for :alert do
      .alert.alert-danger
        An error occurred whilst fetching your bill, please refresh the page to try again.
  - else
    %h3
      Latest Bill
      %small Due #{date_string(resource[:statement][:due])}
      %span.pull-right= currency(resource[:total])
    %hr

      = render 'subscriptions'
      %hr
      = render 'phone_charges'
      %hr
      = render 'tv_charges'

      .pull-right
        = link_to '#', class: 'btn' do
          %i.glyphicon.glyphicon-download
          %span Download PDF
